<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:EstForge.MAUI.Converters"
             x:Class="EstForge.MAUI.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Value Converters -->
            <converters:IsNullConverter x:Key="IsNullConverter" />
            <converters:IsNotNullConverter x:Key="IsNotNullConverter" />
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
            <converters:StringEmptyToBoolConverter x:Key="StringEmptyToBoolConverter" />
            <converters:DecimalToItalianStringConverter x:Key="DecimalToItalianStringConverter" />

            <!-- Color Palette (iOS-inspired blue theme) -->
            <Color x:Key="Primary">#007AFF</Color>
            <Color x:Key="PrimaryDark">#0051D5</Color>
            <Color x:Key="PrimaryLight">#5AC8FA</Color>
            <Color x:Key="Secondary">#5856D6</Color>
            <Color x:Key="Accent">#FF9500</Color>

            <Color x:Key="Gray100">#F2F2F7</Color>
            <Color x:Key="Gray200">#E5E5EA</Color>
            <Color x:Key="Gray300">#D1D1D6</Color>
            <Color x:Key="Gray400">#C7C7CC</Color>
            <Color x:Key="Gray500">#AEAEB2</Color>
            <Color x:Key="Gray600">#8E8E93</Color>

            <Color x:Key="White">#FFFFFF</Color>
            <Color x:Key="Black">#000000</Color>

            <Color x:Key="Success">#34C759</Color>
            <Color x:Key="Warning">#FF9500</Color>
            <Color x:Key="Error">#FF3B30</Color>

            <!-- Fonts -->
            <OnPlatform x:Key="RegularFont" x:TypeArguments="x:String">
                <On Platform="Android" Value="OpenSans-Regular.ttf" />
                <On Platform="iOS" Value="OpenSans-Regular" />
                <On Platform="WinUI" Value="OpenSans-Regular.ttf" />
            </OnPlatform>

            <OnPlatform x:Key="BoldFont" x:TypeArguments="x:String">
                <On Platform="Android" Value="OpenSans-Semibold.ttf" />
                <On Platform="iOS" Value="OpenSans-Semibold" />
                <On Platform="WinUI" Value="OpenSans-Semibold.ttf" />
            </OnPlatform>

            <!-- Styles -->
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="TextColor" Value="{StaticResource White}" />
                <Setter Property="FontFamily" Value="{StaticResource BoldFont}" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="15,10" />
            </Style>

            <Style x:Key="OutlineButton" TargetType="Button">
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="TextColor" Value="{StaticResource Primary}" />
                <Setter Property="BorderColor" Value="{StaticResource Primary}" />
                <Setter Property="BorderWidth" Value="2" />
                <Setter Property="FontFamily" Value="{StaticResource BoldFont}" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="15,10" />
            </Style>

            <Style TargetType="Entry">
                <Setter Property="BackgroundColor" Value="{StaticResource Gray100}" />
                <Setter Property="TextColor" Value="{StaticResource Black}" />
                <Setter Property="FontFamily" Value="{StaticResource RegularFont}" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="PlaceholderColor" Value="{StaticResource Gray500}" />
            </Style>

            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Black}" />
                <Setter Property="FontFamily" Value="{StaticResource RegularFont}" />
                <Setter Property="FontSize" Value="14" />
            </Style>

            <Style x:Key="TitleLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Primary}" />
                <Setter Property="FontFamily" Value="{StaticResource BoldFont}" />
                <Setter Property="FontSize" Value="24" />
            </Style>

            <Style x:Key="SubtitleLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Gray600}" />
                <Setter Property="FontFamily" Value="{StaticResource RegularFont}" />
                <Setter Property="FontSize" Value="16" />
            </Style>

            <Style x:Key="CardFrame" TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{StaticResource White}" />
                <Setter Property="CornerRadius" Value="15" />
                <Setter Property="HasShadow" Value="True" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="Margin" Value="10,5" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
